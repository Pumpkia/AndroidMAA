# NnMaa 开发文档

面向《奇迹暖暖》的 Android ADB 自动化工作台。仓库：https://github.com/Pumpkia/NnMaa

本文只写当前代码里已经落地的能力，以及明确未完成的部分。Excel 只给人看，脚本不读 Excel。

## 1. 现在做到哪一步

### 已完成

- Qt 单窗口工作台：用例录制、用例回放、语义导航、资产（衣橱模板 / 刷关记忆 / 关卡）。
- 用例模型、模块规则、用例库、Maa 回放（短边 720 坐标系）。
- 衣橱模板入库与引用。
- 仓库账本 `jobs/clothing_memory.json`（v2）：目标衣服、原貌→华丽→珍稀、保留/已有、每日次数。
- 仓库决策：按账本缺口挑选短任务；回放成功后写回 `have` / 次数。
- 关卡解析支持 `8-支3`、`公主12-3` 等；切章列表里用任意 `n/m` 进度条作为展开按钮。
- 源码测试可跑：`python -m unittest discover -s tools -p "test_*.py"`。

### 未完成（还不能自动刷）

- `nav_8z3`、`farm_8z3_once`、`evo_base_to_hua`、`evo_hua_to_rare` 四个短用例尚未在真机录成 `.maa_job.json`。
- 真机游戏（Unity + 腾讯渠道）经常不接收 ADB / Maatouch 模拟点击。大厅「切换章节 → 少女 → 十五章上 → 15-3」没有在真机跑通。
- 无障碍树在游戏内几乎是空的，关卡导航目前主要是规则代码，缺真机可用的点击通道。

结论：人可以改账本、录步骤、排队回放；自动进关和自动刷图还卡在真机点击。

## 2. 怎么运行

入口（不要用旧 Tk）：

```powershell
python -m pip install -r tools/requirements.txt
python tools/qt_workbench.py
```

打包版直接双击 `NnMaa.exe`；无界面跑 Pipeline 用 `NnMaa.exe --run [任务名]`。

- Python 3.10+
- Windows 10/11 x64
- 设备开 USB 调试
- 项目自带 `platform-tools\adb.exe`

真机包名（腾讯渠道，已在实机见过）：`com.tencent.tmgp.qjnn`  
`assets/interface.json` 里默认包名仍可能是占位，按渠道改「游戏包名」。

## 3. 架构

```
qt_workbench.py          唯一桌面入口
├ 用例录制 RecordPage
├ 用例回放 PlaybackPage  → job_runner.MaaJobRunner
├ 语义导航 semantic_navigator.py
└ 资产 asset_page.py
     ├ 衣橱模板  asset_model.py
     ├ 刷关记忆  clothing_memory.py
     └ 关卡      stage_model.py + stage_navigator.py

共用数据
├ job_model / job_library     .maa_job.json
├ module_model                录制 / 语义 / 资产 / 自定义
└ app_paths                   所有可写根目录
```

旧文件：`tools/job_editor.py`、`tools/job_runner_app.py` 是 Tk 遗留，不当主入口。

坐标系以 `job_runner.py` 为准：Maa 短边 720。模板和点击都用这套，不要混用手机截图像素。

## 4. 界面与快捷键

| 快捷键 | 页 | 做什么 |
| --- | --- | --- |
| Ctrl+1 | 用例录制 | 截图、框选、点/滑、编排步骤、保存用例 |
| Ctrl+2 | 用例回放 | 用例库、队列、开始/停止、日志 |
| Ctrl+3 | 语义导航 | UI 树扫描、命名区域、生成 OCR 步骤 |
| Ctrl+4 | 资产 | 衣橱图、刷关账本、关卡识别/前往 |

四个页共享当前 ADB 设备和同一套 `jobs/` 数据。不要把 NnMaa 自己的窗口录成游戏界面。

## 5. 数据落在哪

根目录由 `app_paths.py` 决定：

| 运行方式 | 根目录 |
| --- | --- |
| Windows 安装版 | `%LOCALAPPDATA%\NnMaa` |
| 便携 ZIP / `dist/NnMaa` | 程序目录（有 `portable.flag`） |
| 源码运行 | 项目根目录 |

| 内容 | 路径 | 谁写谁读 |
| --- | --- | --- |
| 目标衣服、材料层级、缺口、每日次数 | `jobs/clothing_memory.json` | 开库/界面写；`job_runner` 成功回调写；决策只读这份 |
| 用例步骤 | `jobs/<分类>/<名>.maa_job.json` | 录制保存；回放加载 |
| 模块规则 | `jobs/modules.json` | 工作台 |
| 语义地图 | `jobs/semantic_map.json` | 语义页 |
| 录制模板图 | `resource/image/jobs/` | 录制裁图 |
| 衣橱/关卡模板 | `resource/image/assets/<分类>/` | 资产页导入；步骤里引用相对路径 |
| 失败截图 | `logs/failures/` | 回放失败时 |
| 导出 Pipeline | `exports/` | 录制页「导出」 |

脚本只认记忆文件 + 衣橱资产 + 用例 JSON。不要另起账本。

## 6. 仓库账本口径

文件：`jobs/clothing_memory.json`，`format_version` = 2。默认链是姹紫嫣红。

- `have` 只允许两种写入：开库盘点、短任务成功回调。
- `keep` 是保留件数；可用 = `have - keep`。
- 华丽/珍稀是进化产物，不要写成关卡掉落。
- 关卡号用 `8-支3`（章 8、支线、序号 3）。难度（少女/公主）单独一步，不要焊进坐标。
- 进化 `need`：能 OCR 就覆盖；兜底华丽 5、珍稀 4。
- 跨天：`daily.date` 变化则 `remain` 重置为 `limit`。

决策（`ClothingLedger.next_job`）：

1. 珍稀 `have >= 1` → `DONE`
2. 华丽可用 `>=` 珍稀 need → `evo_hua_to_rare`
3. 原貌可用 `>=` 华丽 need → `evo_base_to_hua`
4. `daily.remain == 0` → `NO_TRIES`
5. 否则 → `farm_8z3_once`

元任务名：`warehouse_nz_rare`。回放时按上面结果去用例库找同名短任务。找不到就报错，不会凭空造步骤。

建议短任务（需自己录制并保存为同名用例）：

| 用例名 | 作用 | 成功回调 |
| --- | --- | --- |
| `nav_8z3` | 切章、进 8-支3 弹窗 | 不改 have |
| `farm_8z3_once` | 过关一次，回到弹窗 | 原貌 have+掉落，remain-1 |
| `evo_base_to_hua` | 原貌进化华丽 | 原貌 have 扣 need（不低于 keep），华丽 +1 |
| `evo_hua_to_rare` | 华丽进化珍稀 | 华丽 have 扣 need（不低于 keep），珍稀 +1 |

用例可设 `memory_callback`，不设则用用例名匹配。

## 7. 关卡识别（代码有，真机未跑通）

`stage_model.py` 解析：`少女5-3`、`公主12-支2`、`8-支3`、`卷 I 10-支2`。

`stage_navigator.py` 计划动作：点「切换章节」→ 点少女/公主 → 滑动列表 → 点该章右侧任意 `n/m`（1/12、4/12、12/12 均可）展开 → 点具体关卡。未解锁章会停。

大厅（已用真机看过，不要录成软件界面）：

- 顶栏体力/金币/钻石
- 中上当前章节名、「下一章」
- 右侧：衣柜、好友、任务、成就、活动等
- 左下：切换难度、切换章节
- 底：完美进度、下一页

进刷关从左下「切换章节」进列表，不是从大厅地图硬滑。

## 8. 真机点击

已实测：

- 一加：系统应用（如 QQ）ADB 点击可用。
- 小米 / OPPO：`adb input tap` 指令成功，但《奇迹暖暖》画面不变。Unity + 腾讯渠道会丢模拟触摸。
- 游戏无障碍节点基本为空，不能靠控件树点按钮。

工作台点击已改为优先 **scrcpy 投屏 + `--mouse=uhid`**：把设备点映射到 scrcpy 窗口再鼠标点击，游戏更容易当成真触控。

- 安装 [scrcpy 2.4+](https://github.com/Genymobile/scrcpy/releases)，加入 PATH，或放到项目 `scrcpy/scrcpy.exe`。
- **录制入口**：scrcpy 画面嵌在工作台左侧，与画布同一窗口。选设备后点「投屏录制」。
- 点击走 scrcpy `--mouse=uhid`。设 `NNMAA_USE_SCRCPY=0` 则退回 `adb input tap`。
- Maa 回放仍走 ADB/Maa 输入。

## 9. Maa 资源

- `assets/interface.json`：启动游戏、每日签到、领取体力、回到主界面。
- `assets/resource/pipeline/login.json`：对应 Pipeline。
- 改这两处后：`python tools/validate_schema.py`。

## 10. 开发约定

见 `AGENTS.md`。摘要：

- 入口只有 `tools/qt_workbench.py`。
- 可写路径以 `app_paths.py` 为准；安装版不要往程序 `assets/` 里写。
- 不要把密码写进 job JSON。
- 发布包必须空用例、空录制图；打完包再恢复本地数据。
- 改路径/模型/回放/打包后跑：`python -m unittest discover -s tools -p "test_*.py"`。

Windows 发布（需 Inno Setup 6）：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\build_release.ps1 -Version <version>
```

## 11. 建议的下一步

1. 在能点击的设备或模拟器上录四个短用例，名称与第 6 节一致。
2. 开库：把游戏里三件衣服的已拥有写入 `clothing_memory.json` 的 `have`，并核对 `daily.remain`。
3. 队列里放 `warehouse_nz_rare`，看决策是否依次 farm / evolve。
4. 真机点击未打通前，不要把关卡导航当成已交付功能。
