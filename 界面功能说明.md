# NnMaa 界面功能说明

## 1. 文档范围

本文档对应 `tools/qt_workbench.py` 提供的 Qt 桌面工作台，说明当前界面中实际可见和可操作的功能。旧版 `tools/job_editor.py` 和 `tools/job_runner_app.py` 中的 Tk 界面不属于当前工作台启动入口。

主窗口包含顶部工作区切换栏、共享 ADB 设备区、用例录制页、用例回放页、语义导航页、资产页和底部状态栏。

## 2. 顶部与共享设备

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 用例录制 | 点击顶部“用例录制” | 切换到截图标记、步骤编排和用例保存界面。 |
| 用例回放 | 点击顶部“用例回放” | 切换到用例搜索、执行队列和日志界面。切换本身不会保存未保存修改。 |
| 语义导航 | 点击顶部“语义导航” | 切换到真机区域扫描、区域命名、页面关系和直接命令界面。 |
| 资产 | 点击顶部“资产” | 切换到奇迹暖暖衣橱分类、模板预览、导入和加入用例界面。 |
| 选择 ADB 设备 | 使用顶部“ADB 设备”下拉框 | 选择已连接且已授权的 Android 真机或模拟器。 |
| 刷新设备 | 点击设备框右侧刷新按钮 | 重新扫描 ADB 设备；有设备时默认选择第一台。 |
| 获取截图 | 点击顶部“截图” | 获取当前设备画面，并同步到录制页和回放页的设备预览。 |
| 投屏录制 | 点击顶部“投屏录制”或录制页“接入投屏” | 把 scrcpy 画面嵌进工作台左侧，可直接在窗口里操作手机；标注仍用「截图」后的中间画布。 |

顶部“截图”取得的录制画面会按 MaaFramework 的短边 720 规则归一化；画布坐标和模板裁图使用这套坐标系，单步预览时再换算回设备物理分辨率。设备未授权、离线或未选择时，截图操作会给出提示。

## 3. 用例录制

### 3.1 截图标记

| 工具 | 操作方式 | 结果 |
| --- | --- | --- |
| 框选识别区 | 选择工具后在截图上拖动 | 记录模板识别区域，并显示绿色矩形。小于 4 像素的区域会被忽略。 |
| 点击位置 | 选择工具后在截图上单击 | 记录点击坐标，并显示黄色圆点。 |
| 滑动轨迹 | 选择工具后在截图上拖动 | 记录滑动起点和终点，并显示蓝色轨迹。 |
| 清除画布标记 | 点击工具栏中的删除图标 | 清除当前识别区域、点击坐标和滑动轨迹。 |

### 3.2 用例操作

| 功能 | 说明 |
| --- | --- |
| 新建 | 创建名为“新用例”的空用例；有未保存修改时先请求确认。 |
| 从用例库打开 | 从当前模式的用例目录选择 `.maa_job.json` 文件并载入。 |
| 保存到用例库 | 首次保存到用例目录的 `<分类>/<用例名>.maa_job.json`；再次保存覆盖当前文件。保存后保留当前步骤属性和步骤列表。 |
| 导出 | 将当前用例导出为 Maa Pipeline JSON；默认目录随安装版、便携版或源码模式变化。 |

“分类”是文本字段。当前 Qt 工作台通过保存路径创建对应目录，不提供单独的分类管理界面。

### 3.3 步骤编排

步骤列表显示序号、名称、动作和状态。选择步骤会把其属性与画面标记载入右侧属性区。

| 操作 | 说明 |
| --- | --- |
| 添加步骤 | 根据当前属性追加新步骤。 |
| 保存选中修改 | 用当前属性覆盖选中的步骤。 |
| 删除 | 立即从当前用例中移除选中步骤，不显示二次确认；保存用例后才会写入磁盘。 |
| 上移 / 下移 | 调整选中步骤的执行顺序。 |
| 在设备上预览此动作 | 对选中步骤执行单步预览；未选择步骤时使用当前属性临时构造步骤。 |

支持的识别类型为 `TemplateMatch`、`OCR` 和 `DirectHit`；支持的动作类型为 `Click`、`Swipe`、`InputText`、`ClickKey` 和 `DoNothing`。

步骤属性包括名称、OCR 文字、输入内容、匹配阈值、按键码、滑动时长、执行前后延迟、识别区域和动作坐标。录制画面、识别区域、动作坐标和模板裁图统一使用短边 720 的 Maa 坐标系；模板匹配步骤会把归一化画面中的框选图片保存到当前模式的模板目录。
### 3.3.1 模块关联与自定义规则

录制页元数据区的“模块”选择决定当前用例归属。新建用例默认是“用例录制”；语义导航生成或转入录制的用例会保留“语义导航”绑定、模块版本和前置用例。模块选择、步骤编辑和保存使用同一份工作台文档，切换页面不会产生第二份隐藏副本。

点击“新建/编辑模块”可以创建或修改自定义模块。模块 ID 必须以小写字母开头，长度为 2-64 位，只能包含小写字母、数字、下划线和短横线；模块至少选择一种用途和一种动作。用途包括“点击”“检查”“识别”，动作包括“点击”“滑动”“输入文本”“按键”“等待”。内置模块不能覆盖，停用模块会保留定义但不能绑定或回放。

规则版本用于识别定义变化。用例保存和回放前都会检查模块存在、启用状态、版本是否匹配，以及每一步的语义用途和动作是否在允许范围内；未知、停用或版本不匹配的用例必须显式修复后才能执行。


## 4. 用例回放

### 4.1 用例库

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 搜索 | 在搜索框输入用例名称 | 按用例名称实时过滤；当前不按分类名搜索。 |
| 刷新 | 点击用例库标题旁的刷新图标 | 重新扫描当前模式用例目录下的 `.maa_job.json` 文件。 |
| 新建用例 | 点击“新建用例” | 创建空用例并切回录制页。 |
| 编辑 | 选择用例后点击“编辑” | 载入用例并切回录制页。 |
| 删除 | 选择用例后点击“删除” | 确认后永久删除用例文件，并从当前执行队列移除该路径。 |
| 加入队列 | 双击用例或点击“添加到执行队列” | 把用例追加到队列末尾。 |

分类节点仅用于分组展示，不能在当前 Qt 工作台中创建、重命名、删除或接收拖放。

### 4.2 执行队列

队列表格显示序号、用例名称、分类和状态。可移除选中项、清空队列，或用上移/下移按钮调整顺序；这些操作不删除用例文件。

点击“开始执行”后，用例按队列顺序运行。开始前必须选择 ADB 设备；每个用例会先解析并执行其前置用例。某个用例在可选重试后仍失败时会记录失败并立即停止队列，尚未执行的队列项标记为“已跳过”。

开始执行时会绑定当时选中的 ADB 设备。执行期间“开始执行”按钮禁用，“停止执行”按钮启用，同时锁定设备选择、刷新、截图、录制页、用例库和队列编辑。停止是安全停止请求，当前任务结束后状态显示为“已停止”。队列表格和右侧日志会同步显示开始、完成、失败和最终成功数量。

“失败自动重试”会在用例失败后额外执行一次；“失败时保存设备截图”会把最终失败时的画面写入当前数据根目录的 `logs/failures/`。无论用例返回失败还是发生异常，最终失败都会停止队列，并把后续队列项标记为“已跳过”。

## 5. 语义导航

### 5.1 扫描与命名

“扫描当前界面”通过 Android UI Automator 读取当前真机界面，并同步获取截图。扫描开始时绑定当前选中的设备并锁定设备选择与刷新，完成或失败后恢复。候选表分别显示文字坐标和最近可点击父区域坐标；单击候选行会在截图中显示双框，扫描和选行不会点击设备。

| 字段 | 说明 |
| --- | --- |
| 页面名称 | 当前界面的稳定名称；已保存页面可直接选择，也可输入新名称。 |
| 区域名称 | 后续命令中使用的主要名称。 |
| 别名 | 使用中文或英文逗号分隔，例如“查找，搜一搜”。 |
| 用途 | “点击”先识别再点击；“检查”和“识别”只验证内容，失败时阻断后续步骤。 |
| 点击后页面 | 点击该区域后预计到达的页面；留空表示不建立页面跳转关系。 |

保存命名后，双击右侧列表中的记录可以修改；删除命名需要确认。语义地图使用临时文件替换方式写入当前用例目录的 `semantic_map.json`，避免写入中断留下不完整文件。

### 5.2 命令生成与路径

命令会忽略“请”“打开”“点击”“进入”等常见前缀，并按区域名称或别名匹配。选择“添加为用例步骤”后，目标在当前页面时生成一个步骤；目标位于其他页面时，系统根据“点击后页面”关系计算最短路径并生成连续步骤。

生成列表可直接保存到用例库，也可转到“用例录制”继续调整识别方式、延迟和步骤顺序。“试点选中区域”只验证一条候选的实际落点；“在设备上预览”执行完整语义命令；两种设备操作开始时都会绑定当前设备并锁定设备选择与刷新，完成后恢复。普通步骤生成不会操作设备。

设备预览定位依次组合 `resource-id`、`content-desc`、可见文字和控件类型；相同特征出现多次时使用归一化文字坐标确定最近区域。实时点击必须在当前界面重新识别到目标，随后使用实时可点击父区域中心；识别失败不会回退旧坐标。语义地图和保存到用例库的语义步骤会持久化 OCR 文字区域与独立点击区域的归一化比例；回放绑定启动设备，先将目标画面按短边 720 归一化，再依据该设备的实际纵横比物化 ROI 和点击区域。识别失败时 MaaFramework 不会执行下一节点。

## 6. 资产

资产页管理奇迹暖暖模板图，目录为当前模式 `resource/image/assets/<分类>/`，写入用例的模板路径为 `assets/<分类>/<文件名>`。

| 功能 | 说明 |
| --- | --- |
| 分类 | 预设发型、连衣裙、外套、上衣、下装、袜子、鞋子、饰品、妆容、翅膀、特效、主界面、关卡、弹窗；也可在导入时输入新分类。 |
| 搜索 | 按资产名称或分类过滤列表。 |
| 导入图片 | 将本地 png/jpg/webp/bmp 复制到所选分类。 |
| 从截图保存 | 把顶部截图得到的当前画面保存为资产；未截图时提示先截图。 |
| 删除 | 确认后删除文件；若有用例引用该模板会在确认框中提示。 |
| 加入当前用例 | 生成 `TemplateMatch` + `Click` 步骤，绑定内置“资产”模块，并切回录制页。 |

资产页内还有“刷关记忆”。脚本只认 `jobs/clothing_memory.json`，不读 Excel。默认账本是姹紫嫣红原貌→华丽→珍稀；`have` 只由开库或短任务成功回调改写。

| 功能 | 说明 |
| --- | --- |
| 新建目标 | 记录想做的衣服或套装部件。 |
| 添加材料 | 给当前衣服增加下级材料；可多层嵌套。 |
| 需要 / 已有 | 缺口 = 需要件数 − 已有件数。 |
| 刷哪关 | 该件衣服的掉落关卡，例如“少女5-3”。 |
| 每日次数 | 该关卡每天可通关次数；同一关卡共用计数。 |
| 通关一次 | 消耗 1 次今日次数；用完后不能再记，跨天清零。 |
| 获得一件 | 已有件数 +1，并更新缺口。 |
| 优先提示 | 有缺口且仍有次数时，优先提示层级更深的材料关卡。 |
| 识别关卡 | 扫描真机界面，识别章节列表、已展开关卡和今日剩余次数。 |
| 前往此关 | 点击左下角「切换章节」，滑动列表，再点该章右侧任意进度 `n/m`（如 `1/12`、`4/12`、`12/12`）展开明细，然后点对应关卡。未解锁章节会停止。 |

记忆写入当前模式用例目录的 `clothing_memory.json`。内置“资产”模块允许点击、检查、识别用途，以及点击、滑动、等待动作。切换到资产页会刷新目录和记忆；执行回放期间该页与其他工作区一样被锁定。

## 7. 状态、确认与退出

| 场景 | 当前行为 |
| --- | --- |
| 后台操作 | 设备扫描、截图、试点和单步预览在后台线程执行，避免阻塞窗口。 |
| 未保存提示 | 窗口标题末尾显示 `*`；新建用例和退出时请求确认。直接切换录制/回放页不会请求确认。 |
| 删除提示 | 删除用例需要确认；删除步骤不需要确认。 |
| 设备操作锁定 | 语义扫描、试点、语义预览和用例回放开始时绑定当时选中的设备，并锁定设备选择与刷新；回放还会锁定截图、录制页、用例库和队列编辑。操作结束或失败后恢复。 |
| 执行中退出 | 回放任务会发送停止请求；语义命令会等待当前短操作完成。完成后窗口自动退出。 |
| 底部状态栏 | 显示 ADB 连接状态和最近一次操作消息。 |

键盘可使用 `Ctrl+1`、`Ctrl+2`、`Ctrl+3`、`Ctrl+4` 切换录制、回放、语义导航与资产，`Ctrl+S` 保存当前用例，`Ctrl+O` 打开用例；用例库双击仍可快速加入队列。

## 8. 数据位置与安全

| 数据 | Windows 安装版 | Windows 便携版 / 源码版 |
| --- | --- | --- |
| 用例 | `%LOCALAPPDATA%\NnMaa\jobs\<分类>\<用例名>.maa_job.json` | `jobs/<分类>/<用例名>.maa_job.json` |
| 模板图片 | `%LOCALAPPDATA%\NnMaa\resource\image\jobs\<用例名>\<步骤名>.png` | `assets/resource/image/jobs/<用例名>/<步骤名>.png` |
| 衣橱资产 | `%LOCALAPPDATA%\NnMaa\resource\image\assets\<分类>\<文件名>` | `assets/resource/image/assets/<分类>/<文件名>` |
| Pipeline | `%LOCALAPPDATA%\NnMaa\exports\<用例名>.json` | 便携版与源码版均为 `exports/<用例名>.json` |
| 失败截图 | `%LOCALAPPDATA%\NnMaa\logs\failures\<时间>-<用例名>.png` | `logs/failures/<时间>-<用例名>.png` |
| 语义地图 | `%LOCALAPPDATA%\NnMaa\jobs\semantic_map.json` | `jobs/semantic_map.json` |
| 刷关记忆 | `%LOCALAPPDATA%\NnMaa\jobs\clothing_memory.json` | `jobs/clothing_memory.json` |

Windows 安装包不含 `portable.flag`，所以即使程序安装到 `Program Files`，所有可写内容仍进入 `%LOCALAPPDATA%\NnMaa`。升级和卸载不会删除这些数据。便携 ZIP 带 `portable.flag`，数据跟随解压目录移动；本地 `dist/NnMaa` 也保持便携模式。

安装版首次启动会从安装目录中的旧版 `jobs/` 与模板目录迁移已有内容，不覆盖目标目录中的同名文件；完整迁移后写入 `.layout-v1.json`。内置 Maa 资源保持只读，用户模板从数据根目录的 `resource/` 作为覆盖层加载。`NNMAA_DATA_DIR` 可为隔离测试或受管环境覆盖数据根目录，并使用安装版的子目录结构。

`InputText` 和 ProjectInterface 输入可能以明文保存，不应填写生产账号密码、Token 或其他敏感信息。页面名、区域名和别名也会写入语义地图，不要在这些字段中填写凭据。删除用例文件不可撤销，操作前应确认其内容不再需要。
