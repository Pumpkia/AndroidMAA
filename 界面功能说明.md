# MaaQQLogin 界面功能说明

## 1. 文档范围

本文档对应 `tools/qt_workbench.py` 提供的 Qt 桌面工作台，说明当前界面中实际可见和可操作的功能。旧版 `tools/job_editor.py` 和 `tools/job_runner_app.py` 中的 Tk 界面不属于当前工作台启动入口。

主窗口包含顶部工作区切换栏、共享 ADB 设备区、用例录制页、用例回放页、语义导航页和底部状态栏。

## 2. 顶部与共享设备

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 用例录制 | 点击顶部“用例录制” | 切换到截图标记、步骤编排和用例保存界面。 |
| 用例回放 | 点击顶部“用例回放” | 切换到用例搜索、执行队列和日志界面。切换本身不会保存未保存修改。 |
| 语义导航 | 点击顶部“语义导航” | 切换到真机区域扫描、区域命名、页面关系和直接命令界面。 |
| 选择 ADB 设备 | 使用顶部“ADB 设备”下拉框 | 选择已连接且已授权的 Android 真机或模拟器。 |
| 刷新设备 | 点击设备框右侧刷新按钮 | 重新扫描 ADB 设备；有设备时默认选择第一台。 |
| 获取截图 | 点击顶部“截图” | 获取当前设备画面，并同步到录制页和回放页的设备预览。 |

设备截图和坐标按 MaaFramework 的短边 720 规则归一化。设备未授权、离线或未选择时，截图操作会给出提示。

## 3. 用例录制

### 3.1 截图标记

| 工具 | 操作方式 | 结果 |
| --- | --- | --- |
| 框选识别区 | 选择工具后在截图上拖动 | 记录模板识别区域，并显示绿色矩形。小于 4 像素的区域会被忽略。 |
| 点击位置 | 选择工具后在截图上单击 | 记录点击坐标，并显示黄色圆点。 |
| 滑动轨迹 | 选择工具后在截图上拖动 | 记录滑动起点和终点，并显示蓝色轨迹。 |
| 清除画布标记 | 点击工具栏中的删除图标 | 清除当前识别区域、点击坐标和滑动轨迹。 |

### 3.2 用例操作

| 功能 | 说明 |
| --- | --- |
| 新建 | 创建名为“新用例”的空用例；有未保存修改时先请求确认。 |
| 从用例库打开 | 从 `jobs/` 选择 `.maa_job.json` 文件并载入。 |
| 保存到用例库 | 首次保存到 `jobs/<分类>/<用例名>.maa_job.json`；再次保存覆盖当前文件。保存后保留当前步骤属性和步骤列表。 |
| 导出 | 将当前用例导出为 Maa Pipeline JSON，默认位置为 `assets/resource/pipeline/`。 |

“分类”是文本字段。当前 Qt 工作台通过保存路径创建对应目录，不提供单独的分类管理界面。

### 3.3 步骤编排

步骤列表显示序号、名称、动作和状态。选择步骤会把其属性与画面标记载入右侧属性区。

| 操作 | 说明 |
| --- | --- |
| 添加步骤 | 根据当前属性追加新步骤。 |
| 保存选中修改 | 用当前属性覆盖选中的步骤。 |
| 删除 | 立即从当前用例中移除选中步骤，不显示二次确认；保存用例后才会写入磁盘。 |
| 上移 / 下移 | 调整选中步骤的执行顺序。 |
| 在设备上预览此动作 | 对选中步骤执行单步预览；未选择步骤时使用当前属性临时构造步骤。 |

支持的识别类型为 `TemplateMatch`、`OCR` 和 `DirectHit`；支持的动作类型为 `Click`、`Swipe`、`InputText`、`ClickKey` 和 `DoNothing`。

步骤属性包括名称、OCR 文字、输入内容、匹配阈值、按键码、滑动时长、执行前后延迟、识别区域和动作坐标。模板匹配步骤会把框选图片保存到 `assets/resource/image/jobs/<用例名>/<步骤名>.png`。

## 4. 用例回放

### 4.1 用例库

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 搜索 | 在搜索框输入用例名称 | 按用例名称实时过滤；当前不按分类名搜索。 |
| 刷新 | 点击用例库标题旁的刷新图标 | 重新扫描 `jobs/` 下的 `.maa_job.json` 文件。 |
| 新建用例 | 点击“新建用例” | 创建空用例并切回录制页。 |
| 编辑 | 选择用例后点击“编辑” | 载入用例并切回录制页。 |
| 删除 | 选择用例后点击“删除” | 确认后永久删除用例文件，并从当前执行队列移除该路径。 |
| 加入队列 | 双击用例或点击“添加到执行队列” | 把用例追加到队列末尾。 |

分类节点仅用于分组展示，不能在当前 Qt 工作台中创建、重命名、删除或接收拖放。

### 4.2 执行队列

队列表格显示序号、用例名称、分类和状态。可移除选中项、清空队列，或用上移/下移按钮调整顺序；这些操作不删除用例文件。

点击“开始执行”后，用例按队列顺序运行。开始前必须选择 ADB 设备；每个用例会先解析并执行其前置用例。某个用例失败后会记录失败，随后继续处理队列中的下一项。

执行期间“开始执行”按钮禁用，“停止执行”按钮启用，同时锁定设备、录制页、用例库和队列编辑。停止是安全停止请求，当前任务结束后状态显示为“已停止”。队列表格和右侧日志会同步显示开始、完成、失败和最终成功数量。

“失败自动重试”会在用例失败后额外执行一次；“失败时保存设备截图”会把最终失败时的画面写入 `logs/failures/`。单个用例发生异常时会记录失败，并继续执行后续队列项。

## 5. 语义导航

### 5.1 扫描与命名

“扫描当前界面”通过 Android UI Automator 读取当前真机界面，列出包含文字、无障碍描述、资源 ID 或点击属性的候选区域。扫描不会点击设备。

| 字段 | 说明 |
| --- | --- |
| 页面名称 | 当前界面的稳定名称；已保存页面可直接选择，也可输入新名称。 |
| 区域名称 | 后续命令中使用的主要名称。 |
| 别名 | 使用中文或英文逗号分隔，例如“查找，搜一搜”。 |
| 用途 | “点击区域”可以执行；“页面标志”仅参与当前页面识别。 |
| 点击后页面 | 点击该区域后预计到达的页面；留空表示不建立页面跳转关系。 |

保存命名后，双击右侧列表中的记录可以修改；删除命名需要确认。语义地图使用临时文件替换方式写入 `jobs/semantic_map.json`，避免写入中断留下不完整文件。

### 5.2 命令生成与路径

命令会忽略“请”“打开”“点击”“进入”等常见前缀，并按区域名称或别名匹配。选择“添加为用例步骤”后，目标在当前页面时生成一个步骤；目标位于其他页面时，系统根据“点击后页面”关系计算最短路径并生成连续步骤。

生成列表可直接保存到用例库，也可转到“用例录制”继续调整识别方式、延迟和步骤顺序。“在设备上预览”才会立即点击真机；普通步骤生成不会操作设备。

设备预览定位依次组合 `resource-id`、`content-desc`、可见文字和控件类型；相同特征出现多次时使用保存位置确定最近区域。如果稳定特征全部失效，才按屏幕宽高比例换算保存坐标。保存到用例库的语义步骤使用短边 720 归一化的 `DirectHit` 坐标，可由现有 MaaFramework 回放队列执行。

## 6. 状态、确认与退出

| 场景 | 当前行为 |
| --- | --- |
| 后台操作 | 设备扫描、截图和单步预览在后台线程执行，避免阻塞窗口。 |
| 未保存提示 | 窗口标题末尾显示 `*`；新建用例和退出时请求确认。直接切换录制/回放页不会请求确认。 |
| 删除提示 | 删除用例需要确认；删除步骤不需要确认。 |
| 执行中锁定 | 设备选择、截图、录制页、用例库和队列编辑暂时禁用，任务结束后恢复。 |
| 执行中退出 | 回放任务会发送停止请求；语义命令会等待当前短操作完成。完成后窗口自动退出。 |
| 底部状态栏 | 显示 ADB 连接状态和最近一次操作消息。 |

键盘可使用 `Ctrl+1`、`Ctrl+2`、`Ctrl+3` 切换录制、回放与语义导航，`Ctrl+S` 保存当前用例，`Ctrl+O` 打开用例；用例库双击仍可快速加入队列。

## 7. 数据位置与安全

| 数据 | 默认位置 |
| --- | --- |
| 用例 | `jobs/<分类>/<用例名>.maa_job.json` |
| 模板图片 | `assets/resource/image/jobs/<用例名>/<步骤名>.png` |
| Pipeline | `assets/resource/pipeline/<用例名>.json`，导出时可改位置 |
| 失败截图 | logs/failures/<时间>-<用例名>.png |
| 语义地图 | `jobs/semantic_map.json` |

`InputText` 和 ProjectInterface 输入可能以明文保存，不应填写生产账号密码、Token 或其他敏感信息。页面名、区域名和别名也会写入语义地图，不要在这些字段中填写凭据。删除用例文件不可撤销，操作前应确认其内容不再需要。
