# MaaQQLogin 界面功能说明

## 1. 文档范围

本文档盘点 `MaaQQLogin - 自动化用例工作台` 主窗口中可见及可交互的功能，包括按钮、输入项、列表、鼠标操作、快捷键、状态提示和确认对话框。

主窗口由以下区域组成：

- 顶部工作区切换栏
- 左侧设备预览区
- 右侧用例录制区
- 右侧用例回放区
- 底部状态栏

## 2. 顶部工作区切换栏

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 用例录制 | 点击“用例录制”或按 `Ctrl+1` | 切换到用例创建、步骤编排和用例库管理界面。 |
| 用例回放 | 点击“用例回放”或按 `Ctrl+2` | 切换到用例搜索、执行队列和运行日志界面。进入前如当前用例有未保存步骤，会先尝试保存。 |
| 当前工作区提示 | 查看高亮按钮 | 当前工作区按钮以高亮样式显示。 |
| 运行中切回录制 | 点击“用例录制” | 如用例正在执行，会询问是否停止当前执行并返回录制界面。 |

## 3. 设备预览区

设备预览区在录制和回放工作区中始终显示，两个工作区共享同一个 ADB 设备。

### 3.1 设备连接

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 选择 ADB 设备 | 使用“ADB 设备”下拉框 | 选择已连接且已授权的 Android 真机或模拟器。 |
| 刷新设备 | 点击“刷新” | 重新扫描 ADB 设备。发现设备后默认选择第一台；没有设备时显示未发现设备。 |
| 获取截图 | 点击“截图” | 获取当前设备画面，并按短边 720 进行 Maa 坐标归一化。 |
| 查看截图 | 查看预览画布 | 截图自动缩放并居中显示，调整窗口大小时会自动重绘。 |

### 3.2 录制标记工具

以下工具只在“用例录制”工作区显示。

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 框选模板 | 选择“框选模板”，在截图上按住左键拖动 | 记录模板识别区域，并生成绿色矩形标记。小于 4 像素的区域会被忽略。 |
| 选择点击位置 | 选择“点击位置”，在截图上单击 | 记录点击坐标，并生成黄色圆形标记。 |
| 记录滑动轨迹 | 选择“滑动轨迹”，在截图上按住左键拖动 | 记录滑动起点和终点，并生成带箭头的蓝色轨迹。 |
| 清除标记 | 点击“清除标记” | 清除当前模板区域、点击位置和滑动轨迹。 |
| 查看识别区域 | 查看步骤属性中的“识别区域” | 显示当前 ROI 坐标；未选择时显示“未框选”。 |
| 查看动作坐标 | 查看步骤属性中的“动作坐标” | 显示点击坐标或滑动起止坐标；未选择时使用识别结果或显示未选择。 |

## 4. 用例录制工作区

### 4.1 用例级操作

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 新建用例 | 点击“新建” | 清空当前用例并恢复默认步骤属性。存在未保存修改时先请求确认。 |
| 设置用例名称 | 编辑“当前用例”输入框 | 设置保存、分类推荐和模板目录使用的用例名称。 |
| 保存用例 | 点击“保存” | 校验用例后保存为 `.maa_job.json`。首次保存自动按分类生成路径；已有路径时覆盖原文件。 |
| 另存为 | 点击“另存为” | 打开文件保存对话框，将当前用例保存到指定位置。 |
| 导出 Pipeline | 点击“导出” | 校验并导出 Maa Pipeline JSON，同时提示入口节点和文件位置。 |
| 保存反馈 | 查看弹窗和底部状态栏 | 保存成功后显示文件路径，并把属性区恢复为可追加新步骤的状态；已保存步骤不会被清空。 |

### 4.2 “步骤编排”页签

#### 步骤列表

步骤列表显示序号、步骤名称、识别方式和动作类型。

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 选择步骤 | 单击步骤列表项 | 把该步骤的全部参数及画面标记载入属性区。 |
| 添加到末尾 | 点击“添加” | 校验当前属性，并把新步骤追加到列表末尾。步骤名称不可重复。 |
| 准备新步骤 | 点击“新步骤” | 取消当前选择并恢复默认值，准备追加新步骤。 |
| 保存选中修改 | 选择步骤后点击“更新” | 用属性区中的内容更新所选步骤。 |
| 删除步骤 | 选择步骤后点击“删除” | 确认后删除所选步骤。 |
| 上移步骤 | 选择步骤后点击“上移” | 将步骤向前移动一位。第一项不可继续上移。 |
| 下移步骤 | 选择步骤后点击“下移” | 将步骤向后移动一位。最后一项不可继续下移。 |

#### 快速添加

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 快速添加输入文本 | 点击“输入文本” | 输入要写入设备的文本，自动创建“直接执行 + 输入文本”步骤。 |
| 快速添加等待 | 点击“等待延迟” | 输入 0 至 600000 毫秒，自动创建“直接执行 + 等待/无动作”步骤。 |

#### 步骤属性

| 字段 | 可选值或格式 | 用途 |
| --- | --- | --- |
| 名称 | 文本 | 步骤名称，用例内不可重复。 |
| 识别 | 模板匹配（找图） | 使用截图中保存的模板进行匹配。 |
| 识别 | 文字识别（OCR） | 按预期文字进行 OCR 识别。 |
| 识别 | 直接执行（无需识别） | 不等待识别结果，直接执行动作。 |
| 动作 | 点击 | 点击固定坐标或模板匹配结果。 |
| 动作 | 滑动 | 按起点、终点和持续时间执行滑动。 |
| 动作 | 输入文本 | 通过 ADB 向设备输入文本。 |
| 动作 | 按键 | 通过 ADB 发送 Android 按键码。 |
| 动作 | 等待 / 无动作 | 不执行设备动作，可配合延迟实现等待。 |
| 输入内容 | 文本 | `InputText` 动作发送的内容。空格会按 ADB 输入规则处理。 |
| OCR 文字 | 文本 | OCR 识别期望命中的文字。 |
| 匹配阈值 | 小数，默认 `0.80` | 模板匹配或识别的最低接受阈值。 |
| 按键码 | 整数，默认 `4` | `ClickKey` 动作使用的 Android keyevent 编号。 |
| 滑动时长 ms | 整数，默认 `300` | 滑动动作持续时间。 |
| 执行前延迟 ms | 整数，默认 `0` | 动作执行前等待时间。 |
| 执行后延迟 ms | 整数，默认 `500` | 动作执行后等待时间。 |
| 识别区域 | 只读坐标 | 显示当前框选的 ROI。 |
| 动作坐标 | 只读坐标 | 显示点击位置或滑动起止点。 |

#### 单步预览

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 在设备上预览此动作 | 选择已保存步骤后点击对应按钮 | 在当前设备上执行一次该步骤，用于验证点击、滑动、输入、按键或延迟效果。 |
| 模板点击预览 | 预览“模板匹配 + 点击”步骤 | 重新截图并匹配模板，匹配分数达到阈值后点击模板中心。 |
| 预览状态 | 查看底部状态栏或错误弹窗 | 显示执行中、成功或失败原因。 |

### 4.3 “用例库”页签

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 选择当前分类 | 使用“当前分类”下拉框 | 指定当前用例保存时所属分类。 |
| 智能推荐分类 | 点击“智能推荐” | 根据用例名称和步骤内容自动推荐分类。 |
| 搜索用例 | 在“搜索用例”中输入文字 | 按键输入时实时过滤共享用例库。 |
| 浏览共享用例库 | 展开分类树 | 按分类查看本地 `.maa_job.json` 用例。 |
| 载入用例 | 选择用例后点击“载入” | 将选中用例载入用例录制区。存在未保存修改时先请求确认。 |
| 双击载入 | 双击用例 | 与“载入”功能相同。 |
| 设为前置 | 选择用例后点击“设为前置” | 把该用例加入当前用例的前置用例列表；不可添加自身或重复项。 |
| 刷新用例库 | 点击“刷新” | 重新扫描磁盘上的用例和分类。 |
| 查看前置顺序 | 查看“前置用例（按顺序执行）”列表 | 显示当前用例执行前需要依次运行的用例。 |
| 移除前置 | 选择前置用例后点击“移除” | 从当前用例中移除该前置引用。 |
| 上移前置 | 选择前置用例后点击“上移” | 提前前置用例的执行顺序。 |
| 下移前置 | 选择前置用例后点击“下移” | 延后前置用例的执行顺序。 |

## 5. 用例回放工作区

### 5.1 回放状态

| 功能 | 说明 |
| --- | --- |
| 设备摘要 | 显示当前共享的 ADB 设备；未选择时显示“尚未连接”。 |
| 执行状态 | 显示就绪、执行中、正在停止、已停止、执行完成或执行失败。 |
| 进度提示 | 显示当前用例序号、名称及最终完成数量。 |

### 5.2 回放用例库

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 实时搜索 | 在“搜索”框输入文字 | 按用例名或分类名实时过滤用例库。按 `Enter` 也可刷新结果。 |
| 刷新 | 点击“刷新” | 重新读取用例和分类，并更新用例/分类数量。 |
| 新建分类 | 点击“新建分类”或按 `Ctrl+N` | 输入分类名称并创建本地分类目录。 |
| 重命名分类 | 选择分类后点击“重命名”或按 `F2` | 重命名分类，并同步更新其中用例及执行队列路径。 |
| 删除用例 | 选择用例后点击“删除”或按 `Delete` | 确认后永久删除用例，并从执行队列移除对应项。 |
| 删除分类 | 选择分类后点击“删除”或按 `Delete` | 删除空分类需一次确认；包含用例的分类需二次确认，并永久删除其中全部用例。 |
| 编辑步骤 | 选择用例后点击“编辑步骤”或按 `Ctrl+E` | 切回用例录制区并载入该用例继续编辑。 |
| 加入执行队列 | 选择用例后点击“加入执行队列” | 将用例追加到队列末尾。 |
| 双击加入 | 双击用例 | 将用例追加到执行队列。 |
| 回车加入 | 选择用例后按 `Enter` | 将用例追加到执行队列。 |
| 拖动用例分类 | 把用例拖到目标分类 | 移动用例文件，并同步更新用例分类、相关前置引用及队列路径。 |
| 查看库统计 | 查看“用例库”右侧计数 | 显示当前筛选结果中的用例数和分类数。 |

### 5.3 执行队列

队列表格显示序号、用例名称、分类和前置用例。

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 移除队列项 | 选择队列项后点击“移除”或按 `Delete` | 从队列中移除所选用例，不删除用例文件。 |
| 上移队列项 | 点击“上移”或按 `Ctrl+↑` | 将所选用例向前移动一位。 |
| 下移队列项 | 点击“下移”或按 `Ctrl+↓` | 将所选用例向后移动一位。 |
| 清空队列 | 点击“清空” | 移除队列中的全部用例，不删除用例文件。 |
| 查看队列数量 | 查看“执行队列”右侧计数 | 显示当前队列项目数。 |

### 5.4 执行日志与控制

| 功能 | 操作方式 | 说明 |
| --- | --- | --- |
| 开始执行 | 点击“开始执行” | 按队列顺序在当前设备上执行用例；每个用例会先按配置执行前置用例。 |
| 停止执行 | 执行中点击“停止” | 请求安全停止当前任务，并等待执行器结束。 |
| 查看实时日志 | 查看“执行日志”文本框 | 显示用例开始、完成、失败、异常、停止和用例库变更信息，并自动滚动到最新内容。 |
| 清空日志 | 点击“清空” | 清空界面上的日志内容，不影响用例或队列。 |
| 失败中止 | 自动行为 | 某个用例失败后停止继续执行后续队列项。 |

### 5.5 运行期间的限制

用例执行期间，为避免文件和队列状态发生冲突，以下功能会被禁用：

- 新建、重命名、删除分类
- 删除用例
- 编辑步骤
- 将用例加入队列
- 移除、上移、下移或清空队列
- 再次开始执行

此时“停止”按钮启用。停止请求发出后，“停止”按钮会再次禁用，直至后台执行结束。

## 6. 状态栏与提示机制

| 功能 | 说明 |
| --- | --- |
| 左侧状态文本 | 显示设备扫描、截图、步骤增删改、保存、导出和预览结果。 |
| 右侧坐标规则 | 固定提示“Maa 坐标：短边 720”。 |
| 后台操作 | 设备扫描、截图和单步预览在后台执行，避免界面阻塞。 |
| 参数校验 | 保存、导出、添加步骤和修改步骤前检查名称、数值、坐标、模板及动作所需字段。 |
| 错误提示 | ADB、文件、模板匹配、校验或执行失败时弹出错误信息。 |
| 未保存确认 | 新建、载入其他用例或关闭程序前，如有未保存修改，会询问是否继续。 |
| 删除确认 | 删除步骤、用例和分类时显示确认；删除含用例的分类需要二次确认。 |
| 退出确认 | 关闭程序时，如回放任务仍在执行，会询问是否停止任务并退出。 |

## 7. 快捷操作汇总

| 快捷操作 | 功能 |
| --- | --- |
| `Ctrl+1` | 切换到用例录制。 |
| `Ctrl+2` | 切换到用例回放。 |
| 回放用例库中双击用例 | 加入执行队列。 |
| 回放用例库中按 `Enter` | 加入执行队列。 |
| 回放用例库中按 `Delete` | 删除选中的用例或分类。 |
| 回放用例库中按 `F2` | 重命名选中的分类。 |
| 回放用例库中按 `Ctrl+E` | 编辑选中用例的步骤。 |
| 回放用例库中按 `Ctrl+N` | 新建分类。 |
| 执行队列中按 `Delete` | 移除选中队列项。 |
| 执行队列中按 `Ctrl+↑` | 上移选中队列项。 |
| 执行队列中按 `Ctrl+↓` | 下移选中队列项。 |
| 用例拖到分类节点 | 移动用例到该分类。 |

## 8. 数据与文件结果

| 操作 | 默认结果位置 |
| --- | --- |
| 保存用例 | `jobs/<分类>/<用例名>.maa_job.json` |
| 保存模板图片 | `assets/resource/image/jobs/<用例名>/<步骤名>.png` |
| 导出 Pipeline | 默认进入 `assets/resource/pipeline/`，也可在保存对话框中选择其他位置。 |

> 注意：`InputText` 的内容会以明文写入用例 JSON，不应填写密码、Token 等敏感信息。
